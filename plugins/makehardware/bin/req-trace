#!/usr/bin/env bash
# MakeHardware wrapper — runs req_trace.py on the hardware toolchain's Python.
exec "${HARDWARE_PYTHON:-/opt/hw-py/bin/python}" \
  "$(dirname "$(dirname "$(readlink -f "$0")")")/scripts/req_trace.py" "$@"
