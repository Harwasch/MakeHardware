#!/usr/bin/env bash
# MakeHardware wrapper — the closed design loop's ledger and evolution chart.
exec "${HARDWARE_PYTHON:-/opt/hw-py/bin/python}" \
  "$(dirname "$(dirname "$(readlink -f "$0")")")/scripts/iterate.py" "$@"
